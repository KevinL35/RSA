# ml

模型与离线流程目录，已归并原 `finetune` 代码与数据流程。

## 当前主要内容

- `configs/`：训练与数据契约配置
- `scripts/`：清洗、切分、训练、评估脚本
- `data/`：原始/清洗/切分数据
- `reports/`：清洗与训练评估报告
- `artifacts/`：模型产物
- `docs/`：模型侧说明文档
- `src/`：训练/推理相关代码

## 兼容说明

- 原 `finetune/README.md` 已保存在 `README-finetune.md`
- 原 `finetune/requirements.txt` 已改名为 `requirements-finetune.txt`
