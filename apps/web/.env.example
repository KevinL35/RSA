# 生产构建 / Vercel：填写完整 API 根地址（无尾斜杠），例如 https://api.example.com
# VITE_API_BASE_URL=https://api.example.com
#
# 本地 npm run dev + 后端在另一台机 / 公网 IP 时：填 http://IP:端口（不要用占位符）
# VITE_API_BASE_URL=http://1.2.3.4:8000
#
# 本地 dev 且后端在本机 8000：可不填，走 vite 代理 /api → http://127.0.0.1:8000
